class Coefficients():
    # For a base neural network
    WEIGHTS = "Weights"
    BIASES = "Biases"

    # For a recurrent neural network
    INPUT_WEIGHTS = "Input Weights"
    INPUT_BIASES = "Input Biases"

    OUTPUT_WEIGHTS = "Output Weights"
    OUTPUT_BIASES = "Output Biases"

    STATE_WEIGHTS = "State Weights"
    STATE_BIASES = "State Biases"
